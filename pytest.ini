[pytest]
testpaths =
    tests
markers =
    backend_agree_test: check that pure Python and Numba backends give the same results.
    backward_compatibility_test: check that current version agrees with previous versions.
    frequency_test: check that results are consistent across different sampling frequencies.